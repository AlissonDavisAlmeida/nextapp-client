import { render, screen } from '@testing-library/react';
import { {{pascalCase name}} } from './index';

describe('{{pascalCase name}} component', () => {
  beforeEach(() => {
    render(<{{pascalCase name}}/>);
  });

  it("should render '{{pascalCase name}}' text", () => {
   
  });

});
